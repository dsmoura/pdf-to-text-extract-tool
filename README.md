Tool for extracting text from PDFs.

It reads the entire path and saves on output file.

Tip: It saves a lot of tokens from any AI LLMs. So, instead of ask AI LLMs read your PDFs files, extract all text from your PDFs using this tool, and use the output for the AI LLMs.